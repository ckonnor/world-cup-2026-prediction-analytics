select
    match_id,
    group_letter,
    match_date_utc,
    venue,
    home_team,
    away_team,
    home_team_model_name,
    away_team_model_name,
    home_team_was_resolved,
    away_team_was_resolved,
    home_historical_matches,
    away_historical_matches,
    home_last_10_points_per_match,
    away_last_10_points_per_match,
    last_10_points_per_match_diff,
    home_last_10_goal_diff_per_match,
    away_last_10_goal_diff_per_match,
    last_10_goal_diff_per_match_diff,
    home_fifa_rank,
    away_fifa_rank,
    fifa_rank_diff,
    home_fifa_points,
    away_fifa_points,
    fifa_points_diff,
    home_squad_status,
    away_squad_status,
    home_squad_players,
    away_squad_players,
    home_overall_star_power_z,
    away_overall_star_power_z,
    overall_star_power_diff,
    home_attack_vs_away_defense_diff,
    away_attack_vs_home_defense_diff,
    home_international_event_matches,
    away_international_event_matches,
    home_matched_club_players,
    away_matched_club_players,
    expected_total_corners,
    expected_total_yellow_cards,
    expected_total_red_cards,
    has_complete_strength_features,
    has_complete_squad_features,
    has_complete_event_features,
    has_complete_fifa_rankings
from {{ ref('features_world_cup_group_matches') }}
