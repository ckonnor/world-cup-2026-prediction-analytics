# External Raw Data

This folder stores reproducible downloads from the martj42 international football results dataset.

Local files generated by `python src/download_external_data.py`:

- `results.csv`
- `shootouts.csv`

The CSV files are ignored by Git. Keep the downloader and source documentation in version control, not the raw data itself.
